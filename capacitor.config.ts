import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'G6_KurmannCyril_Uebungen',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
